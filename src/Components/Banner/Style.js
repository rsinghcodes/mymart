import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  root: {
    // border: "1px solid black",
    marginTop: "4rem",
    minHeight: "60vh",
    display: "grid",
    placeItems: "center",
  },
}));
